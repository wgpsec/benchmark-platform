# Team Authentication System Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add multi-team support with Agent-Token authentication, per-team progress tracking in SQLite, and a Web UI team management page with leaderboard.

**Architecture:** SQLite stores teams and per-team progress. A FastAPI dependency extracts the Agent-Token header (optional — missing token falls back to a "default" team). Challenge objects remain immutable definitions; all mutable state (solved flags, hints) is read/written from DB per-team. Web UI gains a team management page and the dashboard shows a leaderboard.

**Tech Stack:** Python 3.10+, FastAPI, sqlite3 (stdlib), secrets (stdlib), Jinja2 + Alpine.js (frontend)

---

## File Structure

| File | Action | Responsibility |
|------|--------|---------------|
| `benchmark_platform/db.py` | Create | SQLite connection, schema, all DB queries |
| `benchmark_platform/auth.py` | Create | FastAPI dependency for Agent-Token validation |
| `benchmark_platform/server.py` | Modify | Inject team dependency into API routes, replace global state with DB calls |
| `benchmark_platform/web/routes.py` | Modify | Add team management page route and team API routes |
| `benchmark_platform/web/context.py` | Modify | Add leaderboard context builder |
| `benchmark_platform/web/submission_store.py` | Modify | Add team_id/team_name to SubmissionRecord |
| `benchmark_platform/web/templates/pages/teams.html` | Create | Team management page template |
| `benchmark_platform/web/templates/components/sidebar.html` | Modify | Add "队伍管理" nav item |
| `benchmark_platform/web/templates/pages/dashboard.html` | Modify | Add leaderboard section |
| `benchmark_platform/__init__.py` | Modify | Version bump to 0.4.0 |
| `.gitignore` | Modify | Add `data/` |
| `tests/test_db.py` | Create | Tests for DB module |
| `tests/test_auth.py` | Create | Tests for auth dependency |
| `tests/test_team_api.py` | Create | Integration tests for team-scoped API |

---

### Task 1: Database Module

**Files:**
- Create: `benchmark_platform/db.py`
- Create: `tests/test_db.py`

- [ ] **Step 1: Write failing tests for db module**

```python
# tests/test_db.py
import pytest
from pathlib import Path
from benchmark_platform.db import (
    init_db, create_team, list_teams, get_team_by_token,
    get_or_create_default_team, mark_flag_solved,
    get_team_solved_count, is_hint_viewed, mark_hint_viewed,
    get_team_progress, _set_db_path,
)


@pytest.fixture(autouse=True)
def tmp_db(tmp_path):
    """Use a temporary DB for each test."""
    db_path = tmp_path / "test.db"
    _set_db_path(db_path)
    init_db()
    yield db_path


def test_create_team():
    team = create_team("AlphaTeam")
    assert team["name"] == "AlphaTeam"
    assert len(team["token"]) == 32
    assert "id" in team


def test_create_team_duplicate_name():
    create_team("AlphaTeam")
    with pytest.raises(ValueError, match="already exists"):
        create_team("AlphaTeam")


def test_list_teams_empty():
    teams = list_teams()
    assert teams == []


def test_list_teams_with_data():
    create_team("Team1")
    create_team("Team2")
    teams = list_teams()
    assert len(teams) == 2


def test_get_team_by_token():
    team = create_team("AlphaTeam")
    found = get_team_by_token(team["token"])
    assert found is not None
    assert found["name"] == "AlphaTeam"


def test_get_team_by_token_invalid():
    assert get_team_by_token("nonexistent") is None


def test_get_or_create_default_team():
    t1 = get_or_create_default_team()
    t2 = get_or_create_default_team()
    assert t1["id"] == t2["id"]
    assert t1["name"] == "default"


def test_mark_flag_solved():
    team = create_team("Team1")
    mark_flag_solved(team["id"], "XBEN-001-24", "flag1")
    assert get_team_solved_count(team["id"], "XBEN-001-24") == 1


def test_mark_flag_solved_idempotent():
    team = create_team("Team1")
    mark_flag_solved(team["id"], "XBEN-001-24", "flag1")
    mark_flag_solved(team["id"], "XBEN-001-24", "flag1")
    assert get_team_solved_count(team["id"], "XBEN-001-24") == 1


def test_hint_viewed():
    team = create_team("Team1")
    assert is_hint_viewed(team["id"], "XBEN-001-24") is False
    mark_hint_viewed(team["id"], "XBEN-001-24")
    assert is_hint_viewed(team["id"], "XBEN-001-24") is True


def test_get_team_progress():
    team = create_team("Team1")
    mark_flag_solved(team["id"], "XBEN-001-24", "flag1")
    mark_flag_solved(team["id"], "XBEN-001-24", "flag2")
    mark_flag_solved(team["id"], "XBEN-002-24", "default")
    progress = get_team_progress(team["id"])
    assert progress["XBEN-001-24"]["flag1"] is True
    assert progress["XBEN-001-24"]["flag2"] is True
    assert progress["XBEN-002-24"]["default"] is True
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `cd /Users/f0x/pte-project/weaponize/infra/benchmark-platform && python -m pytest tests/test_db.py -v`
Expected: FAIL — module `benchmark_platform.db` does not exist

- [ ] **Step 3: Implement db.py**

```python
# benchmark_platform/db.py
"""SQLite database for team management and per-team progress."""

import secrets
import sqlite3
import uuid
from datetime import datetime, timezone
from pathlib import Path
from threading import local

_DB_PATH = Path("data/benchmark.db")
_local = local()


def _set_db_path(path: Path) -> None:
    """Override DB path (for testing)."""
    global _DB_PATH
    _DB_PATH = path
    if hasattr(_local, "conn"):
        del _local.conn


def _get_conn() -> sqlite3.Connection:
    if not hasattr(_local, "conn") or _local.conn is None:
        _DB_PATH.parent.mkdir(parents=True, exist_ok=True)
        conn = sqlite3.connect(str(_DB_PATH), check_same_thread=False)
        conn.row_factory = sqlite3.Row
        conn.execute("PRAGMA journal_mode=WAL")
        conn.execute("PRAGMA foreign_keys=ON")
        _local.conn = conn
    return _local.conn


def init_db() -> None:
    conn = _get_conn()
    conn.executescript("""
        CREATE TABLE IF NOT EXISTS teams (
            id         TEXT PRIMARY KEY,
            name       TEXT UNIQUE NOT NULL,
            token      TEXT UNIQUE NOT NULL,
            created_at TEXT NOT NULL DEFAULT (datetime('now'))
        );
        CREATE TABLE IF NOT EXISTS team_progress (
            team_id        TEXT NOT NULL,
            challenge_code TEXT NOT NULL,
            flag_id        TEXT NOT NULL,
            solved         INTEGER NOT NULL DEFAULT 0,
            solved_at      TEXT,
            PRIMARY KEY (team_id, challenge_code, flag_id)
        );
        CREATE TABLE IF NOT EXISTS team_hints (
            team_id        TEXT NOT NULL,
            challenge_code TEXT NOT NULL,
            viewed_at      TEXT NOT NULL DEFAULT (datetime('now')),
            PRIMARY KEY (team_id, challenge_code)
        );
    """)
    conn.commit()


def create_team(name: str) -> dict:
    conn = _get_conn()
    team_id = str(uuid.uuid4())
    token = secrets.token_hex(16)
    try:
        conn.execute(
            "INSERT INTO teams (id, name, token) VALUES (?, ?, ?)",
            (team_id, name, token),
        )
        conn.commit()
    except sqlite3.IntegrityError:
        raise ValueError(f"Team '{name}' already exists")
    return {"id": team_id, "name": name, "token": token}


def list_teams() -> list[dict]:
    conn = _get_conn()
    rows = conn.execute("""
        SELECT t.id, t.name, t.token, t.created_at,
               COALESCE(SUM(p.solved), 0) as solved_flags
        FROM teams t
        LEFT JOIN team_progress p ON t.id = p.team_id AND p.solved = 1
        WHERE t.name != 'default'
        GROUP BY t.id
        ORDER BY solved_flags DESC, t.created_at ASC
    """).fetchall()
    return [dict(r) for r in rows]


def get_team_by_token(token: str) -> dict | None:
    conn = _get_conn()
    row = conn.execute(
        "SELECT id, name, token, created_at FROM teams WHERE token = ?",
        (token,),
    ).fetchone()
    return dict(row) if row else None


def get_or_create_default_team() -> dict:
    conn = _get_conn()
    row = conn.execute(
        "SELECT id, name, token, created_at FROM teams WHERE name = 'default'"
    ).fetchone()
    if row:
        return dict(row)
    return create_team("default")


def mark_flag_solved(team_id: str, challenge_code: str, flag_id: str) -> None:
    conn = _get_conn()
    now = datetime.now(timezone.utc).strftime("%Y-%m-%dT%H:%M:%SZ")
    conn.execute(
        "INSERT OR IGNORE INTO team_progress (team_id, challenge_code, flag_id, solved, solved_at) VALUES (?, ?, ?, 1, ?)",
        (team_id, challenge_code, flag_id, now),
    )
    conn.commit()


def get_team_solved_count(team_id: str, challenge_code: str) -> int:
    conn = _get_conn()
    row = conn.execute(
        "SELECT COUNT(*) as cnt FROM team_progress WHERE team_id = ? AND challenge_code = ? AND solved = 1",
        (team_id, challenge_code),
    ).fetchone()
    return row["cnt"]


def is_hint_viewed(team_id: str, challenge_code: str) -> bool:
    conn = _get_conn()
    row = conn.execute(
        "SELECT 1 FROM team_hints WHERE team_id = ? AND challenge_code = ?",
        (team_id, challenge_code),
    ).fetchone()
    return row is not None


def mark_hint_viewed(team_id: str, challenge_code: str) -> None:
    conn = _get_conn()
    conn.execute(
        "INSERT OR IGNORE INTO team_hints (team_id, challenge_code) VALUES (?, ?)",
        (team_id, challenge_code),
    )
    conn.commit()


def get_team_progress(team_id: str) -> dict[str, dict[str, bool]]:
    conn = _get_conn()
    rows = conn.execute(
        "SELECT challenge_code, flag_id, solved FROM team_progress WHERE team_id = ? AND solved = 1",
        (team_id,),
    ).fetchall()
    result: dict[str, dict[str, bool]] = {}
    for r in rows:
        code = r["challenge_code"]
        if code not in result:
            result[code] = {}
        result[code][r["flag_id"]] = bool(r["solved"])
    return result
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd /Users/f0x/pte-project/weaponize/infra/benchmark-platform && python -m pytest tests/test_db.py -v`
Expected: All 11 tests PASS

- [ ] **Step 5: Commit**

```bash
git add benchmark_platform/db.py tests/test_db.py
git commit -m "feat: add SQLite database module for team management"
```

---

### Task 2: Auth Dependency

**Files:**
- Create: `benchmark_platform/auth.py`
- Create: `tests/test_auth.py`

- [ ] **Step 1: Write failing tests for auth module**

```python
# tests/test_auth.py
import pytest
from unittest.mock import patch
from fastapi import HTTPException
from benchmark_platform.auth import get_current_team
from benchmark_platform.db import init_db, create_team, _set_db_path


@pytest.fixture(autouse=True)
def tmp_db(tmp_path):
    db_path = tmp_path / "test.db"
    _set_db_path(db_path)
    init_db()
    yield


@pytest.mark.asyncio
async def test_no_token_returns_default_team():
    team = await get_current_team(agent_token=None)
    assert team["name"] == "default"


@pytest.mark.asyncio
async def test_valid_token_returns_team():
    created = create_team("TestTeam")
    team = await get_current_team(agent_token=created["token"])
    assert team["name"] == "TestTeam"
    assert team["id"] == created["id"]


@pytest.mark.asyncio
async def test_invalid_token_raises_401():
    with pytest.raises(HTTPException) as exc_info:
        await get_current_team(agent_token="bad_token_value")
    assert exc_info.value.status_code == 401
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `cd /Users/f0x/pte-project/weaponize/infra/benchmark-platform && python -m pytest tests/test_auth.py -v`
Expected: FAIL — module `benchmark_platform.auth` does not exist

- [ ] **Step 3: Implement auth.py**

```python
# benchmark_platform/auth.py
"""FastAPI dependency for Agent-Token authentication."""

from fastapi import Header, HTTPException

from benchmark_platform.db import get_or_create_default_team, get_team_by_token


async def get_current_team(agent_token: str | None = Header(None, alias="Agent-Token")) -> dict:
    """Extract and validate Agent-Token header.

    Returns the team dict. If no token provided, returns the default team.
    """
    if agent_token is None:
        return get_or_create_default_team()
    team = get_team_by_token(agent_token)
    if team is None:
        raise HTTPException(
            status_code=401,
            detail={"code": -1, "message": "Invalid Agent-Token", "data": None},
        )
    return team
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd /Users/f0x/pte-project/weaponize/infra/benchmark-platform && python -m pytest tests/test_auth.py -v`
Expected: All 3 tests PASS

- [ ] **Step 5: Commit**

```bash
git add benchmark_platform/auth.py tests/test_auth.py
git commit -m "feat: add Agent-Token auth dependency"
```

---

### Task 3: Inject Auth and Refactor API Routes

**Files:**
- Modify: `benchmark_platform/server.py`
- Modify: `benchmark_platform/web/submission_store.py`
- Modify: `.gitignore`

- [ ] **Step 1: Add `data/` to .gitignore**

Append `data/` to `.gitignore`.

- [ ] **Step 2: Modify server.py — add init_db call and imports**

At the top of `server.py`, add imports:

```python
from fastapi import Depends
from benchmark_platform.auth import get_current_team
from benchmark_platform.db import (
    init_db, get_or_create_default_team,
    mark_flag_solved, get_team_solved_count,
    is_hint_viewed, mark_hint_viewed, get_team_progress,
)
```

In the `serve()` function (the typer CLI entry), after the manager starts, add:

```python
init_db()
get_or_create_default_team()
```

- [ ] **Step 3: Refactor `GET /api/challenges` to use team**

Change the route signature to accept `team: dict = Depends(get_current_team)`. Replace `c.solved_count` and `c.hint_viewed` reads with DB queries:

```python
@app.get("/api/challenges")
async def tch_get_challenges(team: dict = Depends(get_current_team)):
    if manager is None:
        _err("Server not initialized", 503)
        return

    all_challenges = manager.challenges
    challenge_list = []
    for c in all_challenges:
        bm = c.get_benchmark()
        status = manager.get_instance_status(c.challenge_code)
        entrypoint = None
        if status in ("running", "unhealthy"):
            entrypoint = [f"{manager.public_accessible_host}:{p}" for p in c.target_info.port]

        team_solved = get_team_solved_count(team["id"], c.challenge_code)
        hint_viewed = is_hint_viewed(team["id"], c.challenge_code)
        all_solved = team_solved >= c.flag_count

        if c.flag_count > 0:
            per_flag_score = c.points // c.flag_count
            got_score = per_flag_score * team_solved
        else:
            got_score = c.points if all_solved else 0

        challenge_list.append({
            "benchmark_id": c.get_benchmark_id(),
            "title": bm.name,
            "code": c.challenge_code,
            "difficulty": c.difficulty.value,
            "description": bm.description,
            "level": bm.level,
            "total_score": c.points,
            "total_got_score": got_score,
            "flag_count": c.flag_count,
            "flag_got_count": team_solved,
            "flags": [
                {"id": fs.id, "route": fs.route, "description": fs.description,
                 "solved": get_team_solved_count(team["id"], c.challenge_code) > 0
                         and fs.id in (get_team_progress(team["id"]).get(c.challenge_code, {}))}
                for fs in c.flag_states
            ] if c.flag_states else None,
            "hint_viewed": hint_viewed,
            "instance_status": status,
            "entrypoint": entrypoint,
        })

    solved = sum(1 for c in all_challenges if get_team_solved_count(team["id"], c.challenge_code) >= c.flag_count)
    return _ok({
        "current_level": manager.get_current_level(),
        "total_challenges": len(all_challenges),
        "solved_challenges": solved,
        "challenges": challenge_list,
    })
```

- [ ] **Step 4: Refactor `POST /api/start_challenge` to use team**

```python
@app.post("/api/start_challenge")
async def tch_start_challenge(payload: StartChallengeRequest, team: dict = Depends(get_current_team)):
    if manager is None:
        _err("Server not initialized", 503)
        return

    try:
        challenge = manager._find_by_code(payload.code)
    except KeyError:
        _err(f"Challenge {payload.code} not found", 404)
        return

    team_solved = get_team_solved_count(team["id"], challenge.challenge_code)
    if team_solved >= challenge.flag_count:
        return _ok({"already_completed": True}, "该赛题已全部完成，无需再启动实例")

    if manager.get_instance_status(payload.code) in ("running", "unhealthy"):
        entrypoints = [f"{manager.public_accessible_host}:{p}" for p in challenge.target_info.port]
        return _ok(entrypoints, "赛题实例已在运行中")

    try:
        entrypoints = manager.start_challenge_instance(payload.code)
    except Exception as e:
        logger.error("start_challenge failed", action="api", challenge_code=payload.code, error=str(e))
        _err(f"赛题启动失败: {e}", 502)
        return

    return _ok(entrypoints, "赛题实例启动成功")
```

- [ ] **Step 5: Refactor `POST /api/submit` to use team**

```python
@app.post("/api/submit")
async def tch_submit(payload: SubmitFlagRequest, team: dict = Depends(get_current_team)):
    if manager is None:
        _err("Server not initialized", 503)
        return

    try:
        challenge = manager._find_by_code(payload.code)
    except KeyError:
        _err(f"Challenge {payload.code} not found", 404)
        return

    if manager.get_instance_status(payload.code) not in ("running", "unhealthy"):
        _err("赛题实例未运行", 400)
        return

    try:
        answers = challenge.get_expected_answers()
    except Exception as e:
        _err(f"Failed to get expected answers: {e}", 500)
        return

    matched_flag_id = None
    for fid, fval in answers.items():
        if fval == payload.flag:
            matched_flag_id = fid
            break

    is_correct = matched_flag_id is not None

    if is_correct:
        mark_flag_solved(team["id"], challenge.challenge_code, matched_flag_id)

    team_solved = get_team_solved_count(team["id"], challenge.challenge_code)
    all_solved = team_solved >= challenge.flag_count

    # Record submission
    from datetime import datetime, timezone as _tz
    from benchmark_platform.web.submission_store import SubmissionRecord
    if hasattr(app.state, 'submission_store') and app.state.submission_store is not None:
        bm = challenge.get_benchmark()
        app.state.submission_store.add(SubmissionRecord(
            timestamp=datetime.now(_tz.utc).strftime("%Y-%m-%dT%H:%M:%SZ"),
            challenge_code=payload.code,
            benchmark_id=challenge.get_benchmark_id(),
            challenge_name=bm.name,
            flag_id=matched_flag_id,
            flag_value=payload.flag[:8] + "..." + payload.flag[-4:] if len(payload.flag) > 16 else payload.flag,
            correct=is_correct,
            points=challenge.points // challenge.flag_count if is_correct and challenge.flag_count > 0 else 0,
            team_id=team["id"],
            team_name=team["name"],
        ))

    if is_correct:
        per_flag_score = challenge.points // challenge.flag_count if challenge.flag_count > 0 else challenge.points
        msg = f"恭喜！答案正确（{team_solved}/{challenge.flag_count}），获得{per_flag_score}分"
    else:
        msg = "答案错误，请继续尝试"

    return _ok({
        "correct": is_correct,
        "flag_id": matched_flag_id,
        "message": msg,
        "flag_count": challenge.flag_count,
        "flag_got_count": team_solved,
        "all_solved": all_solved,
    })
```

- [ ] **Step 6: Refactor `POST /api/hint` to use team**

```python
@app.post("/api/hint")
async def tch_hint(payload: HintRequest, team: dict = Depends(get_current_team)):
    if manager is None:
        _err("Server not initialized", 503)
        return

    try:
        challenge = manager._find_by_code(payload.code)
    except KeyError:
        _err(f"Challenge {payload.code} not found", 404)
        return

    if manager.get_instance_status(payload.code) not in ("running", "unhealthy"):
        _err("请先启动赛题实例", 400)
        return

    try:
        hint = challenge.get_hint()
    except Exception as e:
        _err(f"Failed to get hint: {e}", 500)
        return

    mark_hint_viewed(team["id"], payload.code)
    return _ok({"code": payload.code, "hint_content": hint})
```

- [ ] **Step 7: Refactor `GET /api/challenges/{code}/progress` to use team**

```python
@app.get("/api/challenges/{code}/progress")
async def tch_get_progress(code: str, team: dict = Depends(get_current_team)):
    if manager is None:
        _err("Server not initialized", 503)
        return

    try:
        challenge = manager._find_by_code(code)
    except KeyError:
        _err(f"Challenge {code} not found", 404)
        return

    progress = get_team_progress(team["id"])
    challenge_progress = progress.get(challenge.challenge_code, {})

    if challenge.flag_states:
        flags_progress = [
            {"id": fs.id, "route": fs.route, "solved": fs.id in challenge_progress, "solved_at": None}
            for fs in challenge.flag_states
        ]
    else:
        flags_progress = [
            {"id": "default", "route": "/", "solved": "default" in challenge_progress, "solved_at": None}
        ]

    team_solved = get_team_solved_count(team["id"], challenge.challenge_code)
    return _ok({
        "challenge_code": code,
        "flags": flags_progress,
        "solved_count": team_solved,
        "total_count": challenge.flag_count,
        "all_solved": team_solved >= challenge.flag_count,
    })
```

- [ ] **Step 8: Add team dep to `POST /api/stop_challenge` and `POST /api/stop_all`**

```python
@app.post("/api/stop_challenge")
async def tch_stop_challenge(payload: StopChallengeRequest, team: dict = Depends(get_current_team)):
    # ... body unchanged ...

@app.post("/api/stop_all")
async def tch_stop_all(team: dict = Depends(get_current_team)):
    # ... body unchanged ...
```

- [ ] **Step 9: Modify SubmissionRecord to include team fields**

In `benchmark_platform/web/submission_store.py`, add to the dataclass:

```python
@dataclass
class SubmissionRecord:
    timestamp: str
    challenge_code: str
    benchmark_id: str
    challenge_name: str
    flag_id: str | None
    flag_value: str
    correct: bool
    points: int
    team_id: str = ""
    team_name: str = ""
```

- [ ] **Step 10: Commit**

```bash
git add benchmark_platform/server.py benchmark_platform/web/submission_store.py .gitignore
git commit -m "feat: inject team auth into API routes, per-team progress via DB"
```

---

### Task 4: Web UI — Team Management Page

**Files:**
- Modify: `benchmark_platform/web/routes.py`
- Create: `benchmark_platform/web/templates/pages/teams.html`
- Modify: `benchmark_platform/web/templates/components/sidebar.html`

- [ ] **Step 1: Add sidebar nav item**

In `sidebar.html`, after the "镜像预热" link and before `</nav>`, add:

```html
    <a href="/web/teams"
       class="w-full flex items-center gap-2 px-2.5 py-2 rounded-lg text-[12px] font-medium transition-all whitespace-nowrap
              {% if page == 'teams' %}bg-gray-900 text-white{% else %}text-gray-600 hover:bg-gray-50 cursor-pointer{% endif %}">
      <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" d="M18 18.72a9.094 9.094 0 0 0 3.741-.479 3 3 0 0 0-4.682-2.72m.94 3.198.001.031c0 .225-.012.447-.037.666A11.944 11.944 0 0 1 12 21c-2.17 0-4.207-.576-5.963-1.584A6.062 6.062 0 0 1 6 18.719m12 0a5.971 5.971 0 0 0-.941-3.197m0 0A5.995 5.995 0 0 0 12 12.75a5.995 5.995 0 0 0-5.058 2.772m0 0a3 3 0 0 0-4.681 2.72 8.986 8.986 0 0 0 3.74.477m.94-3.197a5.971 5.971 0 0 0-.94 3.197M15 6.75a3 3 0 1 1-6 0 3 3 0 0 1 6 0Zm6 3a2.25 2.25 0 1 1-4.5 0 2.25 2.25 0 0 1 4.5 0Zm-13.5 0a2.25 2.25 0 1 1-4.5 0 2.25 2.25 0 0 1 4.5 0Z"/>
      </svg>
      队伍管理
    </a>
```

- [ ] **Step 2: Add page route and API routes in routes.py**

```python
@web_router.get("/teams")
async def page_teams(request: Request):
    from benchmark_platform.db import list_teams
    teams = list_teams()
    return _render(request, "pages/teams.html", {"page": "teams", "teams": teams})


@web_router.post("/api/teams/create")
async def api_create_team(request: Request):
    from benchmark_platform.db import create_team
    body = await request.json()
    name = body.get("name", "").strip()
    if not name:
        return {"code": -1, "message": "队伍名称不能为空", "data": None}
    try:
        team = create_team(name)
    except ValueError as e:
        return {"code": -1, "message": str(e), "data": None}
    return {"code": 0, "message": "创建成功", "data": team}


@web_router.get("/api/teams")
async def api_list_teams(request: Request):
    from benchmark_platform.db import list_teams
    teams = list_teams()
    return {"code": 0, "message": "success", "data": teams}
```

- [ ] **Step 3: Create teams.html template**

```html
{% extends "base.html" %}
{% block title %}队伍管理 — Benchmark Platform{% endblock %}
{% block content %}
<div x-data="teamsPage()" x-init="init()">
  <!-- Header -->
  <div class="flex items-center justify-between mb-6">
    <div class="text-[13px] text-gray-500">
      共 <span x-text="teams.length" class="font-medium text-gray-700">{{ teams|length }}</span> 支队伍
    </div>
    <button @click="showCreate = true"
            class="h-9 px-4 bg-gray-900 text-white text-[12px] font-medium rounded-lg hover:bg-gray-800 transition-colors cursor-pointer">
      创建队伍
    </button>
  </div>

  <!-- Leaderboard Table -->
  <div class="bg-white rounded-xl border border-gray-100 overflow-hidden">
    <table class="w-full">
      <thead class="bg-gray-50 border-b border-gray-100">
        <tr>
          <th class="px-4 py-3 text-left text-[12px] font-medium text-gray-500 w-12">#</th>
          <th class="px-4 py-3 text-left text-[12px] font-medium text-gray-500">队伍名称</th>
          <th class="px-4 py-3 text-left text-[12px] font-medium text-gray-500">Token</th>
          <th class="px-4 py-3 text-left text-[12px] font-medium text-gray-500">解题数</th>
          <th class="px-4 py-3 text-left text-[12px] font-medium text-gray-500">创建时间</th>
        </tr>
      </thead>
      <tbody class="divide-y divide-gray-100">
        <template x-for="(team, idx) in teams" :key="team.id">
          <tr class="hover:bg-gray-50 transition-colors">
            <td class="px-4 py-3 text-[12px] text-gray-500 font-medium" x-text="idx + 1"></td>
            <td class="px-4 py-3 text-[12px] text-gray-900 font-medium" x-text="team.name"></td>
            <td class="px-4 py-3">
              <code class="text-[11px] bg-gray-100 px-1.5 py-0.5 rounded text-gray-600"
                    x-text="team.token.slice(0, 8) + '...'"></code>
              <button @click="copyToken(team.token)"
                      class="ml-1 text-[11px] text-gray-400 hover:text-gray-600 cursor-pointer">复制</button>
            </td>
            <td class="px-4 py-3 text-[12px] text-gray-700" x-text="team.solved_flags + ' flags'"></td>
            <td class="px-4 py-3 text-[12px] text-gray-500" x-text="team.created_at"></td>
          </tr>
        </template>
        <template x-if="teams.length === 0">
          <tr><td colspan="5" class="px-4 py-8 text-center text-[12px] text-gray-400">暂无队伍，点击右上角创建</td></tr>
        </template>
      </tbody>
    </table>
  </div>

  <!-- Create Modal -->
  <div x-show="showCreate" x-cloak
       class="fixed inset-0 z-50 flex items-center justify-center bg-black/30"
       @click.self="showCreate = false">
    <div class="bg-white rounded-xl shadow-xl w-96 p-6">
      <h3 class="text-[14px] font-semibold text-gray-900 mb-4">创建队伍</h3>
      <input x-model="newName" type="text" placeholder="输入队伍名称"
             class="w-full h-9 px-3 text-[12px] border border-gray-200 rounded-lg focus:outline-none focus:ring-1 focus:ring-gray-300"
             @keydown.enter="doCreate()">
      <div class="flex justify-end gap-2 mt-4">
        <button @click="showCreate = false"
                class="h-8 px-3 text-[12px] text-gray-600 border border-gray-200 rounded-lg hover:bg-gray-50 cursor-pointer">取消</button>
        <button @click="doCreate()"
                class="h-8 px-3 text-[12px] text-white bg-gray-900 rounded-lg hover:bg-gray-800 cursor-pointer">创建</button>
      </div>
    </div>
  </div>

  <!-- Token Display Modal -->
  <div x-show="showToken" x-cloak
       class="fixed inset-0 z-50 flex items-center justify-center bg-black/30"
       @click.self="showToken = false">
    <div class="bg-white rounded-xl shadow-xl w-[480px] p-6">
      <h3 class="text-[14px] font-semibold text-gray-900 mb-2">队伍创建成功</h3>
      <p class="text-[12px] text-gray-500 mb-4">请保存以下 Token，它只会显示一次：</p>
      <div class="flex items-center gap-2 bg-gray-50 border border-gray-200 rounded-lg px-3 py-2">
        <code class="flex-1 text-[12px] font-mono text-gray-900 break-all" x-text="createdToken"></code>
        <button @click="copyToken(createdToken)"
                class="text-[11px] text-gray-500 hover:text-gray-700 shrink-0 cursor-pointer">复制</button>
      </div>
      <div class="flex justify-end mt-4">
        <button @click="showToken = false"
                class="h-8 px-3 text-[12px] text-white bg-gray-900 rounded-lg hover:bg-gray-800 cursor-pointer">确定</button>
      </div>
    </div>
  </div>
</div>

<script>
function teamsPage() {
  return {
    teams: [],
    showCreate: false,
    showToken: false,
    newName: '',
    createdToken: '',

    init() { this.fetchTeams(); },

    async fetchTeams() {
      try {
        const res = await fetch('/web/api/teams');
        const data = await res.json();
        if (data.code === 0) this.teams = data.data;
      } catch(e) { console.error(e); }
    },

    async doCreate() {
      const name = this.newName.trim();
      if (!name) return;
      try {
        const res = await fetch('/web/api/teams/create', {
          method: 'POST',
          headers: {'Content-Type': 'application/json'},
          body: JSON.stringify({name}),
        });
        const data = await res.json();
        if (data.code === 0) {
          this.createdToken = data.data.token;
          this.showCreate = false;
          this.showToken = true;
          this.newName = '';
          this.fetchTeams();
        } else {
          this.$dispatch('toast', {type:'error', message: data.message});
        }
      } catch(e) {
        this.$dispatch('toast', {type:'error', message:'创建失败'});
      }
    },

    copyToken(token) {
      navigator.clipboard.writeText(token);
      this.$dispatch('toast', {type:'success', message:'已复制到剪贴板'});
    },
  };
}
</script>
{% endblock %}
```

- [ ] **Step 4: Commit**

```bash
git add benchmark_platform/web/routes.py \
        benchmark_platform/web/templates/pages/teams.html \
        benchmark_platform/web/templates/components/sidebar.html
git commit -m "feat: add team management page with create and leaderboard"
```

---

### Task 5: Dashboard Leaderboard

**Files:**
- Modify: `benchmark_platform/web/context.py`
- Modify: `benchmark_platform/web/templates/pages/dashboard.html`

- [ ] **Step 1: Add leaderboard context to dashboard_context()**

In `benchmark_platform/web/context.py`, add a function:

```python
def leaderboard_context() -> list[dict]:
    """Get team leaderboard data for dashboard."""
    from benchmark_platform.db import list_teams
    return list_teams()
```

Then in `dashboard_context()`, add:

```python
ctx["leaderboard"] = leaderboard_context()
```

- [ ] **Step 2: Add leaderboard section to dashboard.html**

After the existing stats section in `dashboard.html`, add:

```html
<!-- Leaderboard -->
<div class="bg-white rounded-xl border border-gray-100 overflow-hidden">
  <div class="px-4 py-3 border-b border-gray-100">
    <h3 class="text-[13px] font-semibold text-gray-900">队伍排行榜</h3>
  </div>
  <table class="w-full">
    <thead class="bg-gray-50 border-b border-gray-100">
      <tr>
        <th class="px-4 py-2 text-left text-[11px] font-medium text-gray-500 w-10">#</th>
        <th class="px-4 py-2 text-left text-[11px] font-medium text-gray-500">队伍</th>
        <th class="px-4 py-2 text-right text-[11px] font-medium text-gray-500">解题 Flags</th>
      </tr>
    </thead>
    <tbody class="divide-y divide-gray-100">
      {% for team in leaderboard %}
      <tr class="hover:bg-gray-50">
        <td class="px-4 py-2 text-[11px] text-gray-500">{{ loop.index }}</td>
        <td class="px-4 py-2 text-[12px] text-gray-900 font-medium">{{ team.name }}</td>
        <td class="px-4 py-2 text-[12px] text-gray-700 text-right">{{ team.solved_flags }}</td>
      </tr>
      {% endfor %}
      {% if not leaderboard %}
      <tr><td colspan="3" class="px-4 py-4 text-center text-[11px] text-gray-400">暂无队伍数据</td></tr>
      {% endif %}
    </tbody>
  </table>
</div>
```

- [ ] **Step 3: Commit**

```bash
git add benchmark_platform/web/context.py \
        benchmark_platform/web/templates/pages/dashboard.html
git commit -m "feat: add team leaderboard to dashboard"
```

---

### Task 6: Version Bump and Final Verification

**Files:**
- Modify: `benchmark_platform/__init__.py`
- Modify: `pyproject.toml`

- [ ] **Step 1: Bump version to 0.4.0**

`benchmark_platform/__init__.py`:
```python
__version__ = "0.4.0"
```

`pyproject.toml`:
```toml
version = "0.4.0"
```

- [ ] **Step 2: Run full test suite**

Run: `cd /Users/f0x/pte-project/weaponize/infra/benchmark-platform && python -m pytest tests/ -v`
Expected: All tests PASS

- [ ] **Step 3: Manual smoke test**

Start the server and verify:
1. `GET /api/challenges` without token → returns challenges (default team)
2. Create a team via Web UI (`/web/teams`)
3. `GET /api/challenges` with `Agent-Token: <token>` → returns challenges (team-scoped)
4. Submit a flag with token → progress stored per-team
5. Another team can't see first team's progress

- [ ] **Step 4: Commit**

```bash
git add benchmark_platform/__init__.py pyproject.toml
git commit -m "chore: bump version to 0.4.0"
```

---

## Dependencies Between Tasks

```
Task 1 (db.py) → Task 2 (auth.py) → Task 3 (server.py refactor) → Task 4 (Web UI)
                                                                   → Task 5 (Dashboard)
                                                                   → Task 6 (Version bump)
```

Tasks 4 and 5 are independent of each other but both depend on Task 3.
